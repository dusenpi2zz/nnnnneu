# 验证记录

## v0.2 本地验证（2026-09-08）

- Windows / Python 3.12：34 个测试通过、1 个 LFPy 后端测试明确跳过；13 条图形依赖弃用警告。
- 合成钙成像 demo 与重放通过，荧光恢复 RMSE=0.0605028293。
- 方法目录扩展至 26 条，LFPy 使用独立 Linux 环境。后端尚待 GitHub Actions 实际运行，不将本地跳过记录为仿真通过。

## v0.1 历史验证

日期：2026-09-08。验证范围是本库代码与小型演示，不是所有上游软件的安装/运行，也不是科学结论验证。

## 已完成

- Windows、Python 3.12.4；独立 `.venv` 安装成功。
- NumPy 1.26.4、Matplotlib 3.8.4、pytest 7.4.4。
- 33 个测试通过：ROI 手算、分块一致性、ΔF/F 公式、相关矩阵、非法输入、Suite2p 夹具、哈希/版本/源码漂移、失败留痕、目录完整性。
- 合成演示：seed=42，300 帧、3 个非重叠固定 ROI；荧光恢复 RMSE=0.0605028293，预先定义通过线 <0.15。
- 运行重放成功；同输入与数值环境下结果一致。
- 中文命令行检索和方法卡显示可用；输出统一 UTF-8。
- wheel 已构建并安装到独立目标目录；从安装包加载全部 24 张卡片并运行 demo 成功，排除了仅源码目录可用的问题。
- 32 个 Markdown 文件的本地链接与 24 个目录链接检查通过。
- ROI 信号预览图已视觉检查，坐标轴、ROI 标记和轨迹可读。
- 16 个公开 GitHub 仓库元数据快照全部获取，见 source-audit-2026-09-08.json。JinghaoLu/CellReg 的父仓库确认为 zivlab/CellReg。

## 初次验证遇到的问题

Windows 沙箱拒绝 pytest 临时目录及虚拟环境创建，改在允许的本地执行权限下完成。测试读取中文路径 manifest 时漏设 UTF-8，已改为显式编码并重跑通过。运行实现原本已使用 UTF-8。

Matplotlib 3.8.4 与所解析到的 pyparsing 版本产生 13 条弃用警告，当前测试通过；未来升级图形依赖时应重新验证。直接依赖约束见 environments/validated-constraints.txt；初次独立环境完整版本清单见 environments/windows-py312-freeze.txt，此清单限定 Windows/Python 3.12。

## 尚未验证

- 真实 Suite2p 输出的端到端导入；当前只验证手工构造的数值格式夹具。
- 真实钙成像视频的细胞检出、配准、背景分离和活动推断。
- 真实 fMRI、网络零模型、动力系统和可辨识性流程；这些仍是方法卡。
- 跨平台 CI 的实际运行结果；配置已经提供，不能提前写成通过。
- 大规模数据的资源上限、性能和跨硬件逐位一致性。

合成数据验证的是已知掩模、静止 ROI、常量背景和加性噪声下的软件行为。它不支持“细胞检测准确”“可以恢复真实放电”或“研究者已掌握全部方法”等结论。
